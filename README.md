# node_express_generator

    npm install -g express-generator
    express --view=hbs
    npm install
    npm install sequelize -save
    npm install sequelize-cli -save
    touch .sequelizerc
    node_modules/.bin/sequelize init
    npm start
